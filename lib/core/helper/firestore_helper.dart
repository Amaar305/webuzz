import 'package:cloud_firestore/cloud_firestore.dart';

class FirestoreHelper {
  final FirebaseFirestore firebaseFirestore;
  FirestoreHelper(this.firebaseFirestore);

   Future<void> uploadBuzz(Map<String, dynamic> map) async {
  //  final result = await firebaseFirestore.collection('buzz').add(map);
   
  }
}
